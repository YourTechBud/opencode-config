kind: command
frontmatter:
  description: Write or refactor engineering guidance docs from the current conversation
targets:
  opencode:
    frontmatter:
      agent: build
  pi: {}
  claude:
    as: skill
